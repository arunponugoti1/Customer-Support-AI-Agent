apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  labels:
    {{- include "llmproxy.labels" . | nindent 4 }}
  annotations:
    # Workload Identity link to the Google service account.
    iam.gke.io/gcp-service-account: {{ .Values.serviceAccount.gsaEmail }}
