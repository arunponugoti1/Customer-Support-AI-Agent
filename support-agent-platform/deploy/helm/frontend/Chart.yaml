apiVersion: v2
name: frontend
description: Next.js console — single pane (submit tickets, flow, approval queue, cost). Proxies to internal services.
type: application
version: 0.1.0
appVersion: "0.1.0"
