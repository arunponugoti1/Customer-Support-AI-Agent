apiVersion: v2
name: approval
description: Approval service — human-in-the-loop gate + audit log. Risky actions execute only against an approved record.
type: application
version: 0.3.0
appVersion: "0.3.0"
