apiVersion: v2
name: agent
description: Support Agent — LangGraph flow (classify->lookup->retrieve->compose->gate) over the LLM Proxy, traced to Cloud Trace.
type: application
version: 0.5.0
appVersion: "0.5.0"
